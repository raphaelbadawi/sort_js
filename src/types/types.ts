export interface cityData {
  num_dpt: string | number;
  latitude: string | number;
  longitude: string | number;
  nom_ddpt: string;
  nom_commune: string;
  codes_postaux: string | number;
  dist: string | number | undefined;
}
