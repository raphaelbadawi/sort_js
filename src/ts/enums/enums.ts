export enum Operations {
  COMPARE = "compare",
  SWAP = "swap",
}

export enum AlgoNames {
  RANDOM = "random",
  INSERT = "insert",
  SELECT = "select",
  BUBBLE = "bubble",
  SHELL = "shell",
  MERGE = "merge",
  HEAP = "heap",
  QUICK = "quick",
  QUICK3 = "quick3",
}